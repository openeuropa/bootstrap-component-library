# Carousel V1 (deprecated)

Use `@openeuropa/bcl-carousel-v2` for new standalone carousels. V1 remains
available for Gallery and existing consumers; its markup and behavior are
unchanged. Gallery migration is a separate change.
